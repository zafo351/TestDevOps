pipeline {
    agent{label 'master'}

        stages{

            stage('test'){

                steps{
                    
                    dir("build"){
                        sh "mvn clean compile test"
                    }
                }
            }
        }
}